import { PitchDeck } from '@/components/pitch-deck';

export const metadata = {
  title: 'VeilGate — Pitch Deck',
  description: 'Private micropayments on Stellar — a zero-knowledge shielded pool.',
};

export default function PitchDeckPage() {
  return <PitchDeck />;
}
